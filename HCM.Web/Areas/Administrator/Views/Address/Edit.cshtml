@using HCM.Web.ViewModels.Address
@using HCM.Data.Common;
@model AddressEditViewModel
@{
    ViewData["Title"] = "Edit Address";
}


<!-- Edit-->
<form class="form text-left"
      asp-area="Administrator"
      asp-controller="Address"
      asp-action="Edit"
      method="post">Description
    <div asp-validation-summary="All" class="text-danger"></div>
    <div class="modal-body">
        <div class="mb-3">
            <label class="text mb-0 required" asp-for="@Model.Id"></label>
            <span asp-validation-for="@Model.Id" class="text-danger"></span>
            <input asp-for="@Model.Id" class="form-control" readonly />
        </div>
        <div class="mb-3">
            <label class="text mb-0 required" asp-for="@Model.Location"></label>
            <span asp-validation-for="@Model.Location" class="text-danger"></span>
            <input asp-for="@Model.Location" class="form-control" placeholder="Netherlands Antilles Guilder" />
        </div>
    </div>
    <div class="modal-footer">
        <a type="button" class="btn btn-primary"
           asp-area="Administrator"
           asp-controller="Address"
           asp-action="Index">
            <i class="far fa-window-close"></i>
            Close
        </a>
        <button type="submit" class="btn btn-primary">
            <i class="far fa-save"></i>
            Save changes
        </button>
    </div>
</form>

@section Scripts{
    <script src="~/js/evaluationScoreTable.js"></script>
} 